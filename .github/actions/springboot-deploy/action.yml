runs:
  using: "composite"
  steps:
    - name: Debug Inputs
      run: |
        echo "Host: ${{ inputs.ec2-host }}"
        echo "User: ${{ inputs.ec2-user }}"
      shell: bash

    - name: Setup SSH Key
      run: |
        echo "${{ inputs.ec2-key }}" > mvn-key.pem
        chmod 600 mvn-key.pem
      shell: bash

    - name: Copy JAR
      run: |
        scp -o StrictHostKeyChecking=no -o IdentitiesOnly=yes -i mvn-key.pem app.jar ${{ inputs.ec2-user }}@${{ inputs.ec2-host }}:/home/ubuntu/app.jar
      shell: bash

    - name: Deploy App
      run: |
        ssh -o StrictHostKeyChecking=no -o IdentitiesOnly=yes -i mvn-key.pem ${{ inputs.ec2-user }}@${{ inputs.ec2-host }} "
          pkill -f app.jar || true
          nohup java -jar /home/ubuntu/app.jar > app.log 2>&1 &
        "
      shell: bash
